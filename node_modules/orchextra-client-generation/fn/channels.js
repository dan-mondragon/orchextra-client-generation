'use strict';

var setWith = function setWith(w) {

  var withString = "";
  if (typeof w === 'undefined') {
    w = {};
  }

  if (typeof w.client !== 'undefined') {
    withString += "client";
    if (w.client.length > 0) {
      w.client.forEach(function (param) {
        withString += '.' + param;
      });
      withString += ",";
    } else {
      withString += ",";
    }
  }

  if (typeof w.campaign !== 'undefined') {
    withString += "campaign";
    if (w.campaign.length > 0) {
      w.campaign.forEach(function (param) {
        withString += '.' + param;
      });
      withString += ",";
    } else {
      withString += ",";
    }
  }

  return withString;
};

var setFields = function setFields(fields) {
  var fieldsString = "";
  if (typeof fields === 'undefined') {
    fields = [];
  }

  fields.forEach(function (field) {
    fieldsString += field + ',';
  });
  fieldsString = fieldsString.slice(0, -1);

  return fieldsString;
};

module.exports = {
  setWith: setWith,
  setFields: setFields
};