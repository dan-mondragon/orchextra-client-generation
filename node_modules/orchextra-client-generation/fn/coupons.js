'use strict';

var setWith = function setWith(w) {

  var withString = "";

  if (typeof w === 'undefined') {
    w = {};
  }

  if (typeof w.channel !== 'undefined') {
    withString += "channel";
    if (w.channel.length > 0) {
      w.channel.forEach(function (param) {
        withString += '.' + param;
      });
      withString += ",";
    } else {
      withString += ",";
    }
  }

  return withString;
};

var setFields = function setFields(fields) {
  var fieldsString = "";
  if (typeof fields === 'undefined') {
    fields = [];
  }

  fields.forEach(function (field) {
    fieldsString += field + ',';
  });
  fieldsString = fieldsString.slice(0, -1);

  return fieldsString;
};

module.exports = {
  setWith: setWith,
  setFields: setFields
};